rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para coleções do StudioFlow
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
